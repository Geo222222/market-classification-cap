"""
Data pipeline package (live collector → CSV).

This directory is being rebuilt incrementally in Git. A full reference copy of the
previous implementation lives only on your machine under ``progress/data_pipeline/``
(that folder is gitignored and is not pushed to GitHub).
"""

__version__ = "0.0.0-dev"
